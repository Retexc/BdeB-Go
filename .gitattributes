# Auto detect text files and perform LF normalization
* text=auto
*.txt filter=lfs diff=lfs merge=lfs -text
STM/*.txt !text !filter !merge !diff
Exo/Train/*.txt !text !filter !merge !diff
python/** !text !filter !merge !diff
